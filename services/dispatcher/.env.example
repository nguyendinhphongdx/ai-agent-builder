# Server
PORT=3010
NODE_ENV=development

# Auth — shared secret. Unset in local dev means guard is disabled.
# Set in production; callers must send `x-dispatcher-token: <secret>` header.
DISPATCHER_SECRET=

# RabbitMQ — for LOCAL DEV (pnpm dev). Docker compose sets hostname=rabbitmq
# via hardcoded value in docker-compose.yml environment.
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Default retry config (overridable per message)
RETRY_MAX_ATTEMPTS=3
RETRY_BACKOFF_MS=1000
RETRY_BACKOFF_MULTIPLIER=2

# HTTP client
HTTP_TIMEOUT=30000

# Target service URLs — override routes.json (format: {SERVICE_NAME}_URL, hyphens → underscores)
# BACKEND_URL=http://backend:8000
# MAIL_URL=http://mail:3011
# SOCKET_URL=http://socket:4000
# CODE_SANDBOX_URL=http://code-sandbox:8000

# Per-service secrets the dispatcher injects as forward headers (see
# ``src/config/routes.json`` → ``<service>.header``). Must match the
# target service's own guard secret.
SOCKET_API_SECRET=dev-socket-api-secret
