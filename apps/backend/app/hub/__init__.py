"""Agent Hub — publish, browse, and fork agents.

V1 scope: free templates only, self-publish, no reviews, single version per
template (versioning data structures are in place but exposed UI deferred).
"""
