"""MCP server integration — Settings → Integrations panel.

The router (status + preview endpoints used by the Claude Desktop /
Cursor setup page) lives in :mod:`.router`. Matches the package
shape of every other sub-bucket under ``integrations/`` so callers
import from a predictable path.
"""
