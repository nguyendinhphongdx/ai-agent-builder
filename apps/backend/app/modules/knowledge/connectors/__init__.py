"""KB connectors — HTTP layer.

The engine (provider registry, base class, sync orchestration) lives
in :mod:`app.core.kb_connectors`. This package retains the router so
the feature folder stays a one-stop reference for the HTTP surface
of the knowledge bounded context.
"""
