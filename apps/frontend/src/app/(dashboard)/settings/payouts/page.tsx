import { PayoutHistoryView } from "@/features/settings/payouts/views/PayoutHistoryView";

export const metadata = {
  title: "Payment history",
};

export default function PayoutHistoryPage() {
  return <PayoutHistoryView />;
}
