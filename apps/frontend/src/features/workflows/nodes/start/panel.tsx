import type { PanelProps } from "../types";

export default function StartPanel(_props: PanelProps) {
  return (
    <p className="text-xs text-muted-foreground">
      The Start node is the entry point. No configuration needed.
    </p>
  );
}
