/**
 * Public barrel for the triggers feature. Other features (e.g.
 * WorkflowSettingsView) should import from here rather than reaching
 * into `views/`, `components/`, or `providers/`.
 */
export { TriggersView } from "./views/TriggersView";
